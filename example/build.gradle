apply plugin: 'java'
apply plugin: 'war'
apply plugin: 'jetty'

repositories {
    jcenter()
}

dependencies {
    compile 'javax.servlet:javax.servlet-api:3.0.1'
}

dependencies {
    compile rootProject
}
